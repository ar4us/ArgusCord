<Window x:Class="ArgusCordInstaller.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ArgusCordInstaller"
        mc:Ignorable="d"
        Title="ArgusCord Installer" Height="500" Width="420"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Window.Resources>
        <!-- Flat Button Style for Close Button -->
        <Style x:Key="CloseButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Foreground" Value="#9ca3af"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#ef4444"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Premium Install Button Style -->
        <Style x:Key="InstallButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#8b5cf6"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Height" Value="42"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8" BorderBrush="#a78bfa" BorderThickness="0.5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#7c3aed"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#6d28d9"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Premium Uninstall Button Style -->
        <Style x:Key="UninstallButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#f87171"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Height" Value="42"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8" BorderBrush="#f87171" BorderThickness="1.2">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1AF87171"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#33F87171"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Style for ComboBoxItems inside our Premium ComboBox -->
        <Style x:Key="PremiumComboBoxItemStyle" TargetType="ComboBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="4" 
                                Margin="2" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#1b1236"/>
                                <Setter Property="Foreground" Value="#a78bfa"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#8b5cf6"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Premium ComboBox Style -->
        <Style x:Key="PremiumComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="#120c24"/>
            <Setter Property="BorderBrush" Value="#4C8B5CF6"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid x:Name="templateRoot" SnapsToDevicePixels="True">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition MinWidth="{x:Static SystemParameters.VerticalScrollBarWidth}" Width="0"/>
                            </Grid.ColumnDefinitions>
                            
                            <!-- Toggle Button for DropDown -->
                            <ToggleButton x:Name="toggleButton" 
                                          Grid.ColumnSpan="2" 
                                          Background="{TemplateBinding Background}" 
                                          BorderBrush="{TemplateBinding BorderBrush}" 
                                          BorderThickness="{TemplateBinding BorderThickness}" 
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="border" 
                                                Background="{TemplateBinding Background}" 
                                                BorderBrush="{TemplateBinding BorderBrush}" 
                                                BorderThickness="{TemplateBinding BorderThickness}" 
                                                CornerRadius="8"
                                                Height="40">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <!-- Arrow Indicator -->
                                                <Path x:Name="arrow" 
                                                      Grid.Column="1" 
                                                      Fill="#a78bfa" 
                                                      HorizontalAlignment="Center" 
                                                      VerticalAlignment="Center" 
                                                      Margin="0,0,14,0" 
                                                      Data="M 0 0 L 4 4 L 8 0 Z"/>
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="#1b1236"/>
                                                <Setter TargetName="border" Property="BorderBrush" Value="#8b5cf6"/>
                                            </Trigger>
                                            <Trigger Property="IsChecked" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="#120c24"/>
                                                <Setter TargetName="border" Property="BorderBrush" Value="#8b5cf6"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            
                            <!-- Content Presenter for Selected Item -->
                            <ContentPresenter x:Name="contentPresenter" 
                                              Content="{TemplateBinding SelectionBoxItem}" 
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" 
                                              ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}" 
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}" 
                                              Margin="12,0,30,0" 
                                              IsHitTestVisible="False"/>
                            
                            <!-- Popup containing list of items -->
                            <Popup x:Name="presenterSource" 
                                   AllowsTransparency="True" 
                                   Grid.ColumnSpan="2" 
                                   IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}" 
                                   Placement="Bottom" 
                                   PopupAnimation="{DynamicResource {x:Static SystemParameters.ComboBoxPopupAnimationKey}}">
                                <Border x:Name="dropDownBorder" 
                                        Background="#120c24" 
                                        BorderBrush="#8b5cf6" 
                                        BorderThickness="1" 
                                        CornerRadius="8" 
                                        MaxHeight="{TemplateBinding MaxDropDownHeight}" 
                                        MinWidth="{Binding ActualWidth, ElementName=templateRoot}">
                                    <ScrollViewer x:Name="dropDownScrollViewer" SnapsToDevicePixels="True">
                                        <Grid x:Name="grid" RenderOptions.ClearTypeHint="Enabled" Margin="4">
                                            <ItemsPresenter x:Name="itemsPresenter" KeyboardNavigation.DirectionalNavigation="Contained" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                        </Grid>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- Main Window Container -->
    <Border CornerRadius="16" BorderThickness="1.5" Background="#0b0813" BorderBrush="#8b5cf6">
        <Border.Effect>
            <DropShadowEffect BlurRadius="25" Color="#8b5cf6" Opacity="0.4" ShadowDepth="0"/>
        </Border.Effect>

        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- Title Bar -->
                <RowDefinition Height="Auto"/> <!-- Header Logo -->
                <RowDefinition Height="Auto"/> <!-- Branch Selection -->
                <RowDefinition Height="Auto"/> <!-- Actions -->
                <RowDefinition Height="*"/>    <!-- Logs Console -->
                <RowDefinition Height="Auto"/> <!-- Progress / Status -->
            </Grid.RowDefinitions>

            <!-- Custom Title Bar -->
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Installer v1.14" Foreground="#4b5563" FontSize="12" VerticalAlignment="Center" FontWeight="Medium"/>
                <Button Grid.Column="1" Content="✕" Style="{StaticResource CloseButtonStyle}" Click="CloseButton_Click"/>
            </Grid>

            <!-- Header / Logo -->
            <StackPanel Grid.Row="1" HorizontalAlignment="Center" Margin="0,15,0,25">
                <TextBlock HorizontalAlignment="Center" FontSize="36" FontWeight="ExtraBold">
                    <Run Text="Argus" Foreground="#8b5cf6"/><Run Text="Cord" Foreground="#ffffff"/>
                </TextBlock>
                <TextBlock Text="Next-Gen Discord Client Modification" Foreground="#9ca3af" FontSize="13" HorizontalAlignment="Center" Margin="0,4,0,0"/>
            </StackPanel>

            <!-- Selection Box -->
            <StackPanel Grid.Row="2" Margin="0,0,0,25">
                <TextBlock Text="SELECT DISCORD VERSION" Foreground="#a78bfa" FontSize="11" FontWeight="Bold" Margin="0,0,0,8"/>
                <ComboBox x:Name="DiscordBranchComboBox" 
                          Style="{StaticResource PremiumComboBoxStyle}"
                          ItemContainerStyle="{StaticResource PremiumComboBoxItemStyle}"
                          MaxDropDownHeight="200"
                          SelectionChanged="DiscordBranchComboBox_SelectionChanged"/>
                
                <!-- Path Display -->
                <TextBlock x:Name="PathDisplayLabel" Text="Path: Not Detected" Foreground="#6b7280" FontSize="11" Margin="2,6,0,0" TextTrimming="CharacterEllipsis"/>
            </StackPanel>

            <!-- Actions Panel -->
            <Grid Grid.Row="3" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Content="Install" Margin="0,0,10,0" Style="{StaticResource InstallButtonStyle}" Click="InstallButton_Click"/>
                <Button Grid.Column="1" Content="Uninstall" Style="{StaticResource UninstallButtonStyle}" Click="UninstallButton_Click"/>
            </Grid>

            <!-- Log Console -->
            <Border Grid.Row="4" Background="#06040a" BorderBrush="#0DFFFFFF" BorderThickness="1" CornerRadius="8" Padding="10" Margin="0,0,0,15">
                <ScrollViewer x:Name="LogScrollViewer" VerticalScrollBarVisibility="Auto">
                    <TextBlock x:Name="LogTextBlock" Text="" Foreground="#9ca3af" FontFamily="Consolas" FontSize="11" TextWrapping="Wrap"/>
                </ScrollViewer>
            </Border>

            <!-- Progress & Status -->
            <StackPanel Grid.Row="5">
                <ProgressBar x:Name="InstallProgressBar" Height="4" Foreground="#8b5cf6" Background="#120c24" BorderThickness="0" Value="0" Margin="0,0,0,8" Visibility="Collapsed"/>
                <TextBlock x:Name="StatusLabel" Text="Ready to patch." Foreground="#9ca3af" FontSize="12" HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
